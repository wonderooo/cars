copart:
  user: #replace
  password: #replace

proxy:
  host: proxy
  port: 8100

minio:
  url: http://minio:9000
  user: root
  password: secret123

postgres:
  host: postgres
  port: 5432
  user: postgres
  password: secret
  db_name: postgres

kafka:
  url: kafka:29092

loki:
  url: http://loki:3100

data_bright:
  host: "brd.superproxy.io"
  port: 33335
  user: #replace
  password: #replace
  allow_domains:
    - "www.copart.ca"
    - "copart.ca"
    - "www.copart.com"
    - "copart.com"
    - "cs.copart.com"